import SwiftUI

struct CardView: View {
    let card: TravelCard

    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            // Removed pet name display for privacy
            // ...rest of your card UI goes here...
        }
    }
} 